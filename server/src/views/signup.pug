block content
    .signup-form_container 
        form(action = routes.base + routes.signup, method = "post")
            input(type="email", name="email", require=true, placeholder="E-mail")
            input(type="text", name="nick_name")
            input(type="submit", value="Submit")